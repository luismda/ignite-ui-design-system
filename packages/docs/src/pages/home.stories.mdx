import { Meta } from '@storybook/addon-docs'

<Meta title="Home" />

# Ignite UI (@ig-ui)

Design System of the Ignite applications.

##

### Indroduction

In the tokens tab you can find all the standard values ​that are used 
in Ignite applications such as colors, fonts, spacing and much more.
Also, you can download this package:

```bash
  npm i @ig-ui/tokens
```

See more details [this package](https://www.npmjs.com/package/@ig-ui/tokens) in NPM.

Then you can find all components separated into categories and see all 
variants as well as properties. To use these components, download the package below:

```bash
  npm i @ig-ui/react
```

See more details [this package](https://www.npmjs.com/package/@ig-ui/react) in NPM.